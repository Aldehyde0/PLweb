# deeplearning 实施状态

- 实际目录：`D:\python_code\meachine_learning\deeplearning`
- 完整读取：`00`–`29` 共 30 份 Markdown
- 无无法读取文件
- 深度学习方向当前目录：54 个可点击概念
- 本轮映射/覆盖的长文概念：52 个
- 代表性新增独立页：前向传播、训练循环、Dropout、卷积、池化、CNN 形状、PyTorch Tensor 状态、Dataset/DataLoader、Autograd、梯度裁剪、BatchNorm、LayerNorm、AdamW、学习率调度、序列 mask、RNN、LSTM、GRU、Attention、多头注意力、Transformer mask、VAE、GAN、扩散模型、AMP、MNIST 项目。
- 所有长文页支持收藏、已学习、概念重点、章节重点、笔记、来源和访问历史。
- 静态内容新增 `subcategory`、`importance`、`learningObjectives`、`commonQuestions`；用户状态继续由 LearningProvider 保存，没有重复状态源。
- 未单独建页：术语表中的极细粒度别名，以及 `24` 中只作方向概览的 GNN、语音、时序、推荐等内容。它们保留在“深度学习方向地图”，避免脱离文档概览强行扩写。
