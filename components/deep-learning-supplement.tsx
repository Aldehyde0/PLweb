import { CheckCircle2,HelpCircle,Target } from 'lucide-react';
import type { DeepLearningLongFormConcept } from '@/lib/deep-learning-types';
import { ImportantSection } from '@/components/important-section';

export function DeepLearningIntro({content}:{content:DeepLearningLongFormConcept}){return <div className="article-flow deep-learning-intro"><ImportantSection conceptSlug={content.slug} sectionId="learning-objectives" title="学习目标"><ul className="space-y-3">{content.learningObjectives.map((item)=><li key={item} className="flex gap-3"><Target className="mt-1 size-4 shrink-0 text-primary"/><span>{item}</span></li>)}</ul></ImportantSection><ImportantSection conceptSlug={content.slug} sectionId="importance" title="为什么需要这个概念"><div className="definition-callout"><p>{content.background[0]}</p><ul className="mt-3 space-y-2">{content.importance.map((item)=><li key={item} className="flex gap-2"><CheckCircle2 className="mt-1 size-4 shrink-0 text-primary"/>{item}</li>)}</ul></div></ImportantSection></div>}
export function DeepLearningQuestions({content}:{content:DeepLearningLongFormConcept}){return <div className="article-flow deep-learning-questions"><ImportantSection conceptSlug={content.slug} sectionId="common-questions" title="常见问题"><div className="space-y-3">{content.commonQuestions.map((item)=><article key={item.question} className="analysis-card"><div className="analysis-card-icon"><HelpCircle/></div><div><h3 className="text-sm font-semibold">{item.question}</h3><p className="mt-2 text-sm leading-7 text-muted-foreground">{item.answer}</p></div></article>)}</div></ImportantSection></div>}
