import type { CodeExample, FormulaEntry, LongFormConcept, NumericalExample, VariableDefinition } from './long-form-content';

type Input = {
  slug:string; title:string; difficulty?:'入门'|'进阶'|'挑战'; summary:string; definition:string[]; background:string[]; intuition:string[];
  principle:string[]; formulas?:FormulaEntry[]; variables?:VariableDefinition[]; numericalExample?:NumericalExample;
  steps:string[]; code:CodeExample[]; applications:string[]; pitfalls:string[]; prerequisites:string[]; related:string[]; sections:string[];
};
const source={path:'学习资料/learning_tech/02_preprocessing_feature_engineering.md',title:'02｜数据预处理与特征工程',kind:'原始文档' as const};
function make(input:Input):LongFormConcept{return{id:input.slug,slug:input.slug,title:input.title,category:'machine-learning',difficulty:input.difficulty??'入门',summary:input.summary,definition:input.definition,background:input.background,intuition:input.intuition,corePrinciple:input.principle,formulas:input.formulas??[],variableDefinitions:input.variables??[],numericalExample:input.numericalExample,algorithmSteps:input.steps,codeExamples:input.code,codeExplanation:input.code.flatMap((item)=>item.explanation),applications:input.applications,pitfalls:input.pitfalls,prerequisites:input.prerequisites,relatedConcepts:input.related,sourceDocuments:[source],sourceSections:input.sections,extensionNotes:[]}}

export const preprocessingLongForms:LongFormConcept[]=[
  make({slug:'preprocessing-fit-transform-boundary',title:'预处理的 fit / transform 边界',summary:'先划分数据，只在训练数据上学习预处理参数，再把同一规则应用到验证、测试和未来数据。',definition:['预处理中的 fit 会从数据学习状态，例如填补中位数、标准化均值与标准差、One-Hot 类别集合或 PCA 方向；transform 只使用已学状态转换输入。','数据划分不是预处理之后的步骤，而是所有会学习参数的变换之前的边界。'],background:['许多评价泄漏并不发生在分类器内部，而发生在模型之前。即使测试标签未被使用，只要测试特征参与计算均值、类别词表或选择阈值，评价就不再独立。'],intuition:['把测试集想成封存试卷。fit 相当于总结试卷规律，只有训练材料允许被总结；验证和测试只能按已有规则作答。'],principle:['正确顺序是 split → fit preprocessing on train → transform train/validation/test。','交叉验证时，每一折都拥有自己的训练边界，因此预处理器必须放进 Pipeline，让每个训练折独立 fit。'],formulas:[{label:'数据边界',expression:'θprep = fit(Xtrain)，Xsplit′ = transform(Xsplit; θprep)',description:'θprep 只能由训练数据产生，所有数据分块复用它。'}],variables:[{symbol:'θprep',meaning:'预处理器学到的中位数、均值、类别表、主成分等状态。'},{symbol:'Xsplit',meaning:'训练、验证、测试或线上输入之一。'}],steps:['在查看任何整体统计量前确定划分策略。','保留最终测试集。','只把训练数据传给预处理器 fit。','对所有分块调用 transform。','交叉验证时使用完整 Pipeline。','保存预处理器与模型的完整组合。'],code:[{title:'Pipeline 保证折内拟合',language:'scikit-learn',purpose:'让填补、标准化和分类器共享正确的数据边界。',source:`from sklearn.impute import SimpleImputer\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.pipeline import make_pipeline\nfrom sklearn.preprocessing import StandardScaler\n\nmodel = make_pipeline(\n    SimpleImputer(strategy="median"),\n    StandardScaler(),\n    LogisticRegression(max_iter=2000),\n)\nmodel.fit(X_train, y_train)\ny_pred = model.predict(X_test)`,explanation:['fit 时三步都只看到训练集。','predict 对测试集依次调用 transform、transform、predict。'],expectedOutput:'得到长度与 y_test 相同的预测数组；测试集不会改变任何预处理状态。'}],applications:['缺失值填补','数值缩放','类别编码','PCA 与特征选择','交叉验证'],pitfalls:['完整数据 fit_transform 后再划分','测试集重新 fit','手工处理导致训练与预测规则不一致'],prerequisites:['训练/验证/测试职责','fit、transform、predict'],related:['numerical-standardization','missing-value-imputation','sklearn-pipeline-column-transformer','data-leakage'],sections:['§1 先划分，再学习预处理参数','§12 使用 Pipeline 防止泄漏','§14 预处理检查清单']}),
  make({slug:'data-quality-audit',title:'数据质量检查',summary:'在训练前系统检查形状、类型、缺失、重复、异常单位、标签和采集来源。',definition:['数据质量检查是在建模前验证数据是否符合任务契约的过程。它不仅是打印 describe，还包括标签语义、时间边界、来源一致性和预测时可用性。'],background:['模型会忠实学习输入中的错误。单位混用、重复样本跨分割、标签定义漂移或不可能数值，都可能比算法选择更显著地影响结果。'],intuition:['先确认“尺子、答案和样本对应关系”正确，再比较模型；否则更复杂的模型只会更熟练地拟合错误。'],principle:['检查分为结构、数值、标签与来源四层：结构检查 shape/dtype；数值检查缺失、范围、重复与异常；标签检查比例和一致性；来源检查时间、设备、群组和采集变化。'],steps:['核对样本数、特征数和 X/y 对应。','查看每列 dtype 与单位。','统计缺失和重复。','检查分位数、极端值与不可能值。','查看类别分布和标签定义。','记录时间、来源与群组字段。','把发现转成可重复断言和报告。'],code:[{title:'Pandas 数据审计起点',language:'Python',purpose:'生成结构、缺失、重复、描述统计和标签分布。',source:`print(df.shape)\nprint(df.dtypes)\nprint(df.isna().sum())\nprint("duplicates:", df.duplicated().sum())\nprint(df.describe(include="all"))\nprint(df["target"].value_counts(dropna=False))`,explanation:['shape 和 dtype 先验证结构契约。','缺失、重复和标签分布需要分别检查。','describe 发现异常后仍需回到业务含义确认。'],expectedOutput:'输出行列数、各列类型、缺失计数、重复行数量、描述统计和每类样本数。'}],applications:['建模前数据验收','训练/线上数据契约','错误样本追踪'],pitfalls:['只看均值不看单位和来源','把所有极端值直接删除','忽略标签定义变化'],prerequisites:['Pandas 基础','任务定义'],related:['missing-value-imputation','robust-scaling-and-outliers','future-information-leakage'],sections:['§2 数据质量检查','§8 异常值','§14 预处理检查清单']}),
  make({slug:'missing-value-imputation',title:'缺失值处理',summary:'根据特征类型、缺失机制和业务语义，在训练集内学习填补规则并保留必要的缺失信息。',definition:['缺失值处理是把不可直接供模型使用的空值转换为一致输入，同时尽量保留“为什么缺失”这一可能有用的信息。','数值列可用中位数、均值或业务规则填补；类别列可用众数或专门的“缺失”类别。'],background:['直接删除缺失样本会减少数据并可能改变分布。均值对极端值敏感，中位数通常更稳健。某些缺失不是随机的，例如“未填写收入”可能本身与目标相关。'],intuition:['填补不是猜出真实值，而是为模型建立一致的缺失处理约定；必要时再增加一列告诉模型“这里原本缺失”。'],principle:['填补统计量与标准化参数一样，只能由训练数据学习。交叉验证中应放在 Pipeline 内。选择策略需要结合缺失比例、分布、模型能力与业务含义。'],steps:['统计每列缺失比例。','区分数值、类别和结构性缺失。','判断缺失是否可能携带信息。','在训练集拟合填补器。','对其他分块只 transform。','比较不同策略并检查填补后的分布。'],code:[{title:'数值与类别列分别填补',language:'scikit-learn',purpose:'使用 ColumnTransformer 为不同类型列建立独立规则。',source:`from sklearn.compose import ColumnTransformer\nfrom sklearn.impute import SimpleImputer\n\npreprocessor = ColumnTransformer([\n    ("num", SimpleImputer(strategy="median", add_indicator=True), numeric_columns),\n    ("cat", SimpleImputer(strategy="most_frequent"), categorical_columns),\n])\nX_train_ready = preprocessor.fit_transform(X_train)\nX_test_ready = preprocessor.transform(X_test)`,explanation:['median 对极端值通常比 mean 稳健。','add_indicator 保留哪些数值原本缺失。','测试集只 transform。'],expectedOutput:'得到无缺失的数值矩阵；若训练列出现缺失，附加相应缺失指示列。'}],applications:['表格分类与回归','医疗和问卷数据','传感器缺测'],pitfalls:['用完整数据算填补值','把未知类别和缺失混为一谈','不分析高缺失比例特征的业务含义'],prerequisites:['训练/测试边界','数值与类别数据'],related:['data-quality-audit','categorical-encoding','sklearn-pipeline-column-transformer'],sections:['§3 缺失值','§12 使用 Pipeline 防止泄漏','§13 同时处理数值和类别列']}),
  make({slug:'categorical-encoding',title:'类别编码：One-Hot 与有序编码',summary:'把类别值转换为模型可处理的数值，同时避免给无序类别强加不存在的大小关系。',definition:['One-Hot 为每个无序类别建立独立指示列；有序编码只适用于确实存在低、中、高等顺序的类别。'],background:['多数传统模型需要数值输入。若把红、绿、蓝直接编码为 0、1、2，线性或距离模型会误以为类别之间存在距离和倍数关系。'],intuition:['One-Hot 像为每个类别设置独立开关；有序编码则像等级刻度，只有真实存在顺序时才合理。'],principle:['训练阶段学习类别集合和输出列顺序，预测阶段必须复用。OneHotEncoder(handle_unknown="ignore") 可让未知类别产生全零编码而不是直接报错，但仍应监控未知率。'],steps:['识别无序与有序类别。','确定缺失类别处理。','只在训练集拟合编码器。','对验证和测试复用列空间。','记录生成的特征名称。','检查高基数类别带来的维度与稀疏性。'],code:[{title:'One-Hot 编码未知类别',language:'scikit-learn',purpose:'训练时建立类别空间，预测时安全处理新类别。',source:`from sklearn.preprocessing import OneHotEncoder\n\nencoder = OneHotEncoder(\n    handle_unknown="ignore",\n    sparse_output=False,\n)\ntrain = [["red"], ["green"], ["blue"]]\ntest = [["green"], ["yellow"]]\nencoder.fit(train)\nprint(encoder.get_feature_names_out())\nprint(encoder.transform(test))`,explanation:['fit 只从训练集学习 red/green/blue。','yellow 未见过，因此对应所有已知类别列为 0。'],expectedOutput:'列名包含 blue、green、red；green 行有一个 1，yellow 行为全 0。'}],applications:['无序类别特征','线性模型','混合类型 Pipeline'],pitfalls:['无序类别直接编码为 0/1/2','训练和预测独立 get_dummies 导致列不一致','忽略高基数问题'],prerequisites:['类别变量','训练/测试边界'],related:['missing-value-imputation','sklearn-pipeline-column-transformer','feature-construction'],sections:['§4 类别编码','§13 同时处理数值和类别列']}),
  make({slug:'min-max-scaling',title:'Min-Max 归一化',summary:'使用训练集最小值与最大值把特征线性映射到指定范围，典型为 [0,1]。',definition:['Min-Max 缩放保持原始顺序和相对线性位置，把训练集最小值映射到 0、最大值映射到 1。'],background:['某些算法或接口希望输入处于有限范围。与 Z-score 不同，Min-Max 不会使均值为 0，也不以标准差为尺度。'],intuition:['把训练区间两端固定在 0 和 1，中间点按比例放置。未来数据超出训练范围时，结果可以小于 0 或大于 1。'],principle:['min 与 max 只能来自训练集。极端值会拉长区间，把大多数普通样本压缩到很窄范围，因此它对异常值敏感。'],formulas:[{label:'Min-Max 公式',expression:"x'ⱼ = (xⱼ - xⱼ,min) / (xⱼ,max - xⱼ,min)",description:'默认映射到 [0,1]；分母为 0 时是常量特征。'}],variables:[{symbol:'xⱼ,min',meaning:'第 j 列训练集最小值。'},{symbol:'xⱼ,max',meaning:'第 j 列训练集最大值。'}],numericalExample:{title:'把 [10,20,30] 映射到 [0,1]',given:['训练最小值 10','训练最大值 30'],steps:['10 → (10-10)/(30-10)=0','20 → 0.5','30 → 1','未来值 40 → 1.5'],result:'训练集变为 [0,0.5,1]，未来值可以超过目标范围。',comparison:['Z-score 使用均值和标准差，结果无固定上下界。','Min-Max 使用端点，保持有限训练范围但更怕极端值。']},steps:['先划分数据。','在训练集拟合 min/max。','检查常量列和异常值。','转换所有分块。','保存 scaler 与模型。'],code:[{title:'MinMaxScaler',language:'scikit-learn',purpose:'安全保存训练区间并转换未来数据。',source:`from sklearn.preprocessing import MinMaxScaler\n\nscaler = MinMaxScaler()\ntrain = [[10.0], [20.0], [30.0]]\nfuture = [[40.0]]\nprint(scaler.fit_transform(train).ravel())\nprint(scaler.transform(future).ravel())`,explanation:['fit_transform 只用于训练数据。','future 超出训练最大值，因此得到 1.5。'],expectedOutput:'[0. 0.5 1.]，随后 [1.5]。'}],applications:['有明确有限输入范围的模型','像素或传感器特征缩放'],pitfalls:['以为未来结果一定在 [0,1]','存在异常值仍直接使用','测试集重新拟合端点'],prerequisites:['最大值与最小值','训练数据边界'],related:['numerical-standardization','robust-scaling-and-outliers'],sections:['§5 Min-Max 归一化']}),
  make({slug:'robust-scaling-and-outliers',title:'Robust Scaling 与异常值处理',difficulty:'进阶',summary:'用中位数和四分位距降低极端值对缩放参数的影响，并先区分错误数据与真实稀有事件。',definition:['RobustScaler 通常用训练集中位数做中心、四分位距 IQR 做尺度。异常值处理则是在业务确认后修正、截断、变换、保留或单独建模。'],background:['均值、标准差、最小值和最大值都可能被极端值显著改变。但异常值可能是录入错误，也可能是真实故障、欺诈或高价值对象，不能仅凭数值大就删除。'],intuition:['中位数和中间 50% 数据像一把不容易被少数极端点拉歪的尺子。它提高稳健性，但不会自动消除异常值。'],principle:['IQR=Q3-Q1。RobustScaler 使用训练统计量；处理决策应基于来源、单位和任务语义。长尾非负特征还可考虑 log1p。'],formulas:[{label:'稳健缩放',expression:"x' = (x - mediantrain) / IQRtrain",description:'IQR 是训练集第 75 百分位与第 25 百分位之差。'}],variables:[{symbol:'Q1',meaning:'训练集第 25 百分位。'},{symbol:'Q3',meaning:'训练集第 75 百分位。'},{symbol:'IQR',meaning:'Q3-Q1，中间 50% 数据的跨度。'}],numericalExample:{title:'极端值如何影响尺度',given:['数据 [10,11,12,13,1000]'],steps:['均值为 209.2，被 1000 显著拉高。','中位数为 12。','中间数据跨度远小于全范围 990。'],result:'Robust Scaling 的中心更接近普通样本，但是否删除 1000 仍需判断其来源。',comparison:['StandardScaler 适合近似对称且异常较少的数据。','Min-Max 对端点最敏感。','RobustScaler 对极端点更稳健。']},steps:['定位异常值及来源记录。','检查单位、录入和传感器状态。','确认是否属于目标总体。','选择修正、截断、变换、稳健缩放或保留。','所有数据驱动阈值只在训练集拟合。','比较处理前后验证表现与业务影响。'],code:[{title:'RobustScaler 与 StandardScaler 对比',language:'scikit-learn',purpose:'观察极端值对两种缩放器的影响。',source:`import numpy as np\nfrom sklearn.preprocessing import RobustScaler, StandardScaler\n\nX = np.array([[10.], [11.], [12.], [13.], [1000.]])\nprint(StandardScaler().fit_transform(X).ravel().round(3))\nprint(RobustScaler().fit_transform(X).ravel().round(3))`,explanation:['StandardScaler 使用均值和标准差。','RobustScaler 使用中位数和 IQR。'],expectedOutput:'两组不同缩放值；RobustScaler 下普通样本间的差异不会被 1000 压缩得同样严重。'}],applications:['长尾金额','含故障尖峰的传感器','稳健线性/距离模型'],pitfalls:['看到异常就删除','把 RobustScaler 当异常检测器','用全数据分位数设截断阈值'],prerequisites:['中位数与四分位数','数据质量检查'],related:['data-quality-audit','numerical-standardization','min-max-scaling','skewed-and-cyclical-features'],sections:['§5 数值标准化与特征缩放','§8 异常值']}),
  make({slug:'learning-rate-selection',title:'学习率选择',difficulty:'进阶',summary:'学习率决定每次沿负梯度方向移动的距离，应在特征缩放后按对数尺度比较。',definition:['学习率 α 是梯度更新的步长超参数。它不由普通 fit 自动学习，需要通过训练曲线与验证过程选择。'],background:['量纲差异会让不同参数方向的梯度尺度悬殊，迫使统一学习率变小。标准化通常能让损失地形更均衡，从而允许更稳定的步长。'],intuition:['学习率像下山步幅：过大跨过谷底并震荡，过小移动缓慢，合适时损失快速平滑下降。'],principle:['候选值通常按 10 倍尺度尝试；训练损失用于判断优化状态，验证集用于选择泛化更好的设置，测试集不能用于调参。'],formulas:[{label:'梯度更新',expression:'θₜ₊₁ = θₜ - α∇J(θₜ)',description:'α 控制一步移动距离。'}],variables:[{symbol:'α',meaning:'学习率。'},{symbol:'∇J',meaning:'当前参数处损失上升最快方向。'}],steps:['先缩放数值特征。','选择对数间隔候选值。','固定其他条件训练。','记录每轮训练/验证损失。','识别发散、震荡、过慢与平稳区间。','用验证而非测试选择。'],code:[{title:'按对数尺度比较学习率',language:'NumPy',purpose:'记录不同 α 的损失轨迹。',source:`candidate_rates = [0.0001, 0.001, 0.01, 0.1, 1.0]\nfor alpha in candidate_rates:\n    _, _, losses = gradient_descent(X_scaled, y_train, alpha)\n    print(alpha, losses[-1], np.isfinite(losses).all())`,explanation:['使用相同的已标准化训练数据。','有限性检查可发现 inf/nan 发散。'],expectedOutput:'每个学习率的最终损失和是否保持有限；过大 α 可能发散，过小 α 收敛慢。'}],applications:['线性回归梯度下降','神经网络优化'],pitfalls:['特征未缩放就比较学习率','只跑固定轮数不看曲线','用测试集挑 α'],prerequisites:['梯度下降','数值标准化'],related:['numerical-standardization','gradient-descent-convergence','gradient-descent'],sections:['§6 学习率的选择','§7 判断梯度下降是否收敛']}),
  make({slug:'gradient-descent-convergence',title:'梯度下降收敛诊断',difficulty:'进阶',summary:'结合损失趋势、相对变化、梯度范数、耐心轮数和最大轮数判断优化是否稳定。',definition:['收敛表示在当前模型、特征和优化设置下，参数更新带来的目标改善已经很小；它不等于模型泛化良好。'],background:['运行固定 epochs 既可能提前停止，也可能浪费计算。某一轮变化小也可能只是震荡瞬间，因此需要阈值与 patience。'],intuition:['不是看到车速慢一次就说已到终点，而是连续多次变化都很小，并确认没有发散或停在错误原因上。'],principle:['相对损失变化对不同损失量级更可比；梯度范数接近 0 是补充信号。训练和验证曲线还用于区分优化收敛与过拟合。'],formulas:[{label:'相对损失变化',expression:'rₜ = |Jₜ-Jₜ₋₁| / max(|Jₜ₋₁|, ε)',description:'连续若干轮小于 tolerance 时可提前停止。'}],variables:[{symbol:'ε',meaning:'防止分母为零的小常数。'},{symbol:'patience',meaning:'允许连续稳定或未改善的轮数。'}],steps:['每轮记录训练损失。','检查 loss 和参数是否有限。','计算相对损失变化。','可同时计算梯度范数。','连续 patience 轮满足条件才停止。','设置最大轮数。','用验证曲线确认没有过拟合。'],code:[{title:'耐心机制判断稳定',language:'Python',purpose:'避免一次偶然的小变化导致过早停止。',source:`tolerance = 1e-8\npatience = 10\nstable_count = 0\n\nrelative_change = abs(old_loss - new_loss) / max(abs(old_loss), 1e-12)\nstable_count = stable_count + 1 if relative_change < tolerance else 0\n\nif stable_count >= patience:\n    print("梯度下降已经收敛")`,explanation:['使用相对变化适配不同损失量级。','stable_count 在变化重新变大时清零。'],expectedOutput:'连续 10 轮相对变化小于 1e-8 后打印收敛提示。'}],applications:['手写梯度下降','早停诊断','优化器调试'],pitfalls:['把训练收敛等同泛化成功','不设最大轮数','忽略 nan/inf'],prerequisites:['损失函数','学习率'],related:['learning-rate-selection','overfitting-diagnosis'],sections:['§7 判断梯度下降是否收敛']}),
  make({slug:'class-imbalance',title:'类别不平衡',difficulty:'进阶',summary:'少数类任务中准确率可能严重误导，需要合适指标、阈值、权重和严格的折内重采样。',definition:['类别不平衡表示不同类别样本数量差异明显，常伴随不同错误代价。'],background:['990 个正常与 10 个异常时，全预测正常已有 99% 准确率，但异常召回率为 0。'],intuition:['如果真正重要的是少数异常，不能用“总体答对多少”掩盖“关键问题一个都没找到”。'],principle:['关注 Precision、Recall、F1、PR-AUC、混淆矩阵和 Macro-F1；阈值影响误报漏报权衡。重采样只能在训练集或每个训练折内发生。'],formulas:[{label:'Precision',expression:'TP/(TP+FP)',description:'预测为正的样本中有多少是真的。'},{label:'Recall',expression:'TP/(TP+FN)',description:'真实正类中找到了多少。'},{label:'F1',expression:'2PR/(P+R)',description:'Precision 与 Recall 的调和平均。'}],steps:['统计每类数量。','明确误报和漏报成本。','选择主指标。','建立 Dummy 基线。','在训练折内使用 class_weight 或重采样。','在验证数据上选阈值。','最终测试并分群分析。'],code:[{title:'类别权重与分类报告',language:'scikit-learn',purpose:'训练时提高少数类权重并查看逐类结果。',source:`from sklearn.linear_model import LogisticRegression\nfrom sklearn.metrics import classification_report\n\nmodel = LogisticRegression(class_weight="balanced", max_iter=2000)\nmodel.fit(X_train, y_train)\ny_pred = model.predict(X_test)\nprint(classification_report(y_test, y_pred))`,explanation:['balanced 根据训练类别频率设置权重。','报告 Precision、Recall、F1 和 support。'],expectedOutput:'每个类别各自的 Precision、Recall、F1，以及 macro/weighted 平均。'}],applications:['欺诈检测','疾病筛查','故障告警'],pitfalls:['只报告 Accuracy','先对完整数据重采样','用测试集选阈值'],prerequisites:['混淆矩阵','分类指标'],related:['classification-metrics','baseline-models','cv-pipeline-leakage'],sections:['§9 类别不平衡','§14 预处理检查清单']}),
  make({slug:'feature-construction',title:'领域、比率、多项式与交互特征',difficulty:'进阶',summary:'把原始字段组合为更贴近任务机制的特征，让简单模型也能表达有意义的非线性关系。',definition:['特征构造是定义映射 φ(x)，把原始输入转为模型实际学习的表示。'],background:['线性模型对 φ(x) 仍是线性的，但 φ 可以包含比率、平方项与交互项，使模型表达弯曲或共同作用。'],intuition:['总消费和订单数是原材料，“平均客单价”把业务关系直接告诉模型；平方项让直线拥有弯曲能力。'],principle:['所有特征必须在预测时可获得、定义稳定且无未来信息。高阶多项式会快速增加维度，应结合正则化和交叉验证。'],formulas:[{label:'特征映射',expression:'ŷ = wᵀφ(x)+b',description:'模型实际学习转换后的特征。'},{label:'二阶示例',expression:'ŷ=w₀+w₁x₁+w₂x₂+w₃x₁x₂+w₄x₁²',description:'交互项表达共同作用，平方项表达曲率。'}],steps:['从任务机制提出候选特征。','处理分母为零和单位。','在训练/预测共享的函数中实现。','将变换放入 Pipeline。','比较基线与新增特征的交叉验证结果。','检查维度、稳定性和泄漏。'],code:[{title:'构造比率和二阶特征',language:'Python',purpose:'展示领域比率与 PolynomialFeatures。',source:`import numpy as np\nfrom sklearn.preprocessing import PolynomialFeatures\n\naverage_order_value = total_spending / np.maximum(order_count, 1)\nX_domain = np.column_stack([total_spending, order_count, average_order_value])\npoly = PolynomialFeatures(degree=2, include_bias=False)\nX_train_poly = poly.fit_transform(X_train)\nX_test_poly = poly.transform(X_test)`,explanation:['分母零的业务语义需要明确。','PolynomialFeatures 在训练集 fit，测试集只 transform。'],expectedOutput:'领域特征新增平均客单价；二阶展开产生原特征、平方项和两两交互项。'}],applications:['房价比率特征','用户行为强度','非线性回归'],pitfalls:['构造预测时不存在的特征','高阶展开导致维度爆炸','相关性被误当因果'],prerequisites:['线性模型','Pipeline'],related:['skewed-and-cyclical-features','future-information-leakage','feature-selection'],sections:['§10 特征工程与特征构造']}),
  make({slug:'skewed-and-cyclical-features',title:'偏态与周期特征变换',difficulty:'进阶',summary:'用 log1p 缓和非负长尾分布，用正弦/余弦保留小时、星期等周期相邻关系。',definition:['偏态变换改变数值分布形状；周期编码把首尾相接的变量映射到单位圆。'],background:['金额和次数常有长尾；小时直接编码 0～23 会错误地认为 23 与 0 很远。'],intuition:['log1p 压缩大值而保留顺序；sin/cos 把钟表时间放在圆上，让 23 点与 0 点自然接近。'],principle:['log1p 适用于非负值；周期 T 的编码为 sin(2πx/T)、cos(2πx/T)，两列共同确定相位。'],formulas:[{label:'长尾变换',expression:"x'=log(1+x)",description:'非负数据中压缩大值。'},{label:'周期编码',expression:'xsin=sin(2πx/T)，xcos=cos(2πx/T)',description:'T 是周期长度。'}],steps:['检查变量定义与范围。','非负长尾比较 log1p 前后分布。','确定周期 T。','同时生成 sin/cos 两列。','在验证集比较效果。'],code:[{title:'金额与小时变换',language:'NumPy',purpose:'实现文档中的 log1p 和 24 小时周期编码。',source:`import numpy as np\n\namount_log = np.log1p(amount)\nhour_sin = np.sin(2 * np.pi * hour / 24)\nhour_cos = np.cos(2 * np.pi * hour / 24)`,explanation:['log1p 在接近 0 时更稳定。','周期必须用两列，否则无法唯一表示位置。'],expectedOutput:'一个压缩后的金额特征，以及范围在 [-1,1] 的 hour_sin/hour_cos。'}],applications:['金额与访问次数','小时、星期与季节'],pitfalls:['对含负数数据直接 log1p','只用 sin 或 cos 一列','没有明确真实周期'],prerequisites:['对数函数','三角函数'],related:['feature-construction','robust-scaling-and-outliers'],sections:['§10 偏态特征变换','§10 日期和周期特征']}),
  make({slug:'future-information-leakage',title:'未来信息泄漏',difficulty:'进阶',summary:'输入包含预测时尚未发生的信息，会制造无法上线复现的虚高离线效果。',definition:['未来信息泄漏是特征在训练样本中可见，但在真实预测时点不可获得，或其计算窗口越过预测时点。'],background:['预测贷款违约时使用发放后的催收次数、预测流失时使用未来 7 天行为，都会把结果线索直接放入输入。'],intuition:['这不是模型聪明，而是题目把未来答案印在了当前特征上。'],principle:['每个特征必须标记事件时间、可用时间和计算窗口。划分方式也要模拟线上：过去训练、未来验证；同一实体的历史聚合不能跨越预测时点。'],steps:['定义明确预测时点。','列出每个特征的产生与可用时间。','检查窗口是否跨越预测时点。','按时间或群组划分。','移除泄漏特征后重新建立基线。','记录线上可用性契约。'],code:[{title:'按时间构造过去窗口',language:'Python',purpose:'表达特征截止时间必须早于预测时点。',source:`cutoff = prediction_time\nhistory = events[events["event_time"] < cutoff]\nfeatures = history.groupby("user_id").agg(\n    past_30d_count=("event_id", "count"),\n)`,explanation:['先按预测时点截断，再聚合。','真实项目还需限定过去 30 天的开始时间。'],expectedOutput:'每个用户只由预测时点之前的事件生成特征。'}],applications:['信用风险','用户流失','时间序列预测'],pitfalls:['随机划分掩盖时间泄漏','使用目标发生后的字段','全历史聚合越过截止点'],prerequisites:['训练测试划分','时间数据'],related:['data-leakage','time-series-cross-validation','feature-construction'],sections:['§10 最危险的特征：未来信息','§14 预处理检查清单']}),
  make({slug:'feature-selection',title:'特征选择',difficulty:'进阶',summary:'删除低价值、冗余或不稳定特征，降低方差、成本与解释负担，并把选择过程限制在训练折内。',definition:['特征选择从候选输入中保留一部分列；它不同于 PCA 等特征提取，后者创建新的组合变量。'],background:['特征过多会增加训练成本和偶然相关性。常量、重复、噪声和预测时昂贵的列可能降低泛化或工程可用性。'],intuition:['不是给模型越多信息越好，而是保留稳定、有用、可获得且成本合理的信息。'],principle:['可用方法包括低方差/冗余删除、统计检验、L1、树重要性、递归消除和基于 CV 的选择。任何数据驱动选择必须在训练折内部完成。'],steps:['删除不可用和泄漏特征。','删除常量、重复和明显冗余。','选择与任务匹配的方法。','放入 Pipeline。','用相同交叉验证比较性能与稳定性。','记录保留特征和采集成本。'],code:[{title:'Pipeline 内使用 L1 选择',language:'scikit-learn',purpose:'避免先看完整数据再选择特征。',source:`from sklearn.feature_selection import SelectFromModel\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.pipeline import make_pipeline\nfrom sklearn.preprocessing import StandardScaler\n\nmodel = make_pipeline(\n    StandardScaler(),\n    SelectFromModel(LogisticRegression(penalty="l1", solver="liblinear", C=0.1)),\n    LogisticRegression(max_iter=2000),\n)`,explanation:['标准化让 L1 惩罚更公平。','每个交叉验证训练折独立完成选择。'],expectedOutput:'模型只把被训练折 L1 选择器保留的列传给最终分类器。'}],applications:['高维表格','稀疏文本','降低采集成本'],pitfalls:['完整数据选择后再 CV','把重要性解释为因果','高度相关特征选择不稳定'],prerequisites:['交叉验证','正则化'],related:['l1-regularization','feature-construction','cv-pipeline-leakage'],sections:['§11 特征选择','§12 使用 Pipeline 防止泄漏']}),
  make({slug:'sklearn-pipeline-column-transformer',title:'Pipeline 与 ColumnTransformer',difficulty:'进阶',summary:'用 Pipeline 固化步骤顺序，用 ColumnTransformer 为不同列类型建立独立处理分支。',definition:['Pipeline 把一系列 transform 和最终 estimator 组合成一个可 fit/predict 的对象；ColumnTransformer 按列选择不同变换后拼接结果。'],background:['真实表格同时含数值、类别与缺失。若训练和预测靠手工脚本分别处理，很容易出现顺序、列空间和统计参数不一致。'],intuition:['ColumnTransformer 像分流器，数值走填补+缩放，类别走填补+One-Hot；Pipeline 再把汇合后的特征送入模型。'],principle:['所有有状态步骤都随 Pipeline 在训练折内 fit；保存完整 Pipeline 可同时保存预处理器和分类器。参数搜索用“步骤名__参数名”访问内部配置。'],steps:['列出数值和类别列。','为每类列定义子 Pipeline。','用 ColumnTransformer 组合。','与模型组成总 Pipeline。','在训练数据 fit。','用 CV 搜索内部参数。','保存完整对象并验证加载预测。'],code:[{title:'混合列完整 Pipeline',language:'scikit-learn',purpose:'实现文档中的数值和类别双分支。',source:`from sklearn.compose import ColumnTransformer\nfrom sklearn.impute import SimpleImputer\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.preprocessing import OneHotEncoder, StandardScaler\n\nnumeric_pipeline = Pipeline([\n    ("imputer", SimpleImputer(strategy="median")),\n    ("scaler", StandardScaler()),\n])\ncategorical_pipeline = Pipeline([\n    ("imputer", SimpleImputer(strategy="most_frequent")),\n    ("one_hot", OneHotEncoder(handle_unknown="ignore")),\n])\npreprocessor = ColumnTransformer([\n    ("numeric", numeric_pipeline, numeric_columns),\n    ("categorical", categorical_pipeline, categorical_columns),\n])\nmodel = Pipeline([\n    ("preprocessor", preprocessor),\n    ("classifier", LogisticRegression(max_iter=2000)),\n])`,explanation:['两个分支分别处理列类型。','总 Pipeline 保证训练、验证、测试与保存加载一致。'],expectedOutput:'fit 后模型可直接接收包含原始列名的 DataFrame，并输出预测。'}],applications:['混合类型表格','交叉验证与调参','模型保存交付'],pitfalls:['手工对训练和测试分别 get_dummies','忘记未知类别','只保存分类器不保存预处理'],prerequisites:['缺失值','类别编码','标准化'],related:['preprocessing-fit-transform-boundary','missing-value-imputation','categorical-encoding','numerical-standardization'],sections:['§12 使用 Pipeline 防止泄漏','§13 同时处理数值和类别列']}),
];
